import { FastifyInstance } from "fastify";

export async function setupSwagger(app: FastifyInstance): Promise<void> {
  // TODO: Register Swagger plugin and configuration
  console.log("✅ Swagger ready");
}
